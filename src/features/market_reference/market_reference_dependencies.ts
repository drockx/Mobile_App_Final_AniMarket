import { regionalMarkets } from './data/market_reference_data';

/** Sample references for the prototype; replace with verified market data before production use. */
export const marketReferenceData = regionalMarkets;
